<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.settings.DictionaryManagerActivity"
            android:label="@string/title_activity_dictionary_manager"
            android:exported="false"
            android:parentActivityName=".view.MainActivity" >
            <intent-filter>
                <action android:name="com.koli.openvocab.action.OPEN_DICTIONARY_MANAGER" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.quiz.QuizActivity"
            android:label="@string/title_activity_quiz"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.dictionary.DictionaryActivity"
            android:label="@string/title_activity_dictionary"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".sync.view.login.LoginActivity"
            android:label="@string/title_activity_login" />
    </application>

</manifest>